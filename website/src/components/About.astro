<section id="nosotros" class="about">
  <div class="contenido">
    <p class="eyebrow">Nosotros</p>

    <h2>
      Tecnología con propósito, diseñada para necesidades reales.
    </h2>

    <div class="about-text">
      <p>
        En Lonquensur desarrollamos soluciones tecnológicas simples,
        modernas y confiables, buscando que la tecnología sea una
        herramienta útil y accesible.
      </p>

      <p>
        Trabajamos con una mirada práctica, combinando experiencia,
        innovación y cercanía para transformar necesidades concretas
        en soluciones que realmente funcionan.
      </p>
    </div>
  </div>
</section>

<style>
  .about {
    width: 100%;
    padding: 7rem 2rem;
    background: var(--color-background);
  }

  .contenido {
    max-width: 1100px;
    margin: 0 auto;
  }

  .eyebrow {
    margin: 0 0 1rem;
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 0.25rem;
    font-size: 0.8rem;
    font-weight: 700;
  }

  h2 {
    max-width: 800px;
    margin: 0 0 3rem;
    color: var(--color-text);
    font-size: clamp(2rem, 4vw, 3.2rem);
    line-height: 1.15;
    letter-spacing: -0.03em;
  }

  .about-text {
    max-width: 750px;
  }

  .about-text p {
    margin: 0 0 1.5rem;
    color: var(--color-text-light);
    font-size: 1.05rem;
    line-height: 1.8;
  }

  @media (max-width: 800px) {
    .about {
      padding: 5rem 1.5rem;
    }
  }
</style>