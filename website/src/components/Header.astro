<header class="site-header">
  <div class="header-content">
    <a href="/" class="brand">LONQUENSUR</a>

    <nav class="main-nav">
      <a href="/">Inicio</a>
      <a href="#servicios">Servicios</a>
      <a href="#nosotros">Nosotros</a>
      <a href="#contacto">Contacto</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    width: 100%;
    padding: 1.2rem 1.5rem;
    background: rgba(255, 255, 255, 0.92);
    border-bottom: 1px solid rgba(23, 56, 74, 0.12);
  }

  .header-content {
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .brand {
    color: #17384a;
    text-decoration: none;
    font-size: 1.15rem;
    font-weight: 700;
    letter-spacing: 0.08em;
  }

  .main-nav {
    display: flex;
    align-items: center;
    gap: 1.8rem;
  }

  .main-nav a {
    color: #17384a;
    text-decoration: none;
    font-size: 0.95rem;
    transition: opacity 0.2s ease;
  }

  .main-nav a:hover {
    opacity: 0.65;
  }

  @media (max-width: 700px) {
    .header-content {
      flex-direction: column;
      gap: 1rem;
    }

    .main-nav {
      gap: 1.2rem;
      flex-wrap: wrap;
      justify-content: center;
    }
  }
</style>