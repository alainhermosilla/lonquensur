<section class="hero">
  <div class="hero-image" aria-hidden="true">
    <img
      src="/images/hero-lonquensur.png"
      alt=""
    />
  </div>

  <div class="contenido">
    <p class="subtitulo">Tecnología con propósito</p>

    <div class="marca-hero">
      LONQUEN<span>S</span>UR
    </div>

    <div class="propuesta">
      <p class="frase-principal">
        Primero entendemos el problema.
      </p>

      <p class="frase-secundaria">
        Después pensamos en la tecnología.
      </p>
    </div>

    <p class="descripcion">
      Desarrollamos soluciones tecnológicas simples y útiles para personas,
      empresas y organizaciones que necesitan resolver problemas reales.
    </p>

    <a href="#nosotros" class="boton">
      Conócenos
    </a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    background:
      radial-gradient(
        circle at 35% 38%,
        rgba(255, 255, 255, 0.99) 0%,
        rgba(242, 247, 253, 0.98) 52%,
        rgba(224, 235, 247, 1) 100%
      );
    padding: 2rem;
    box-sizing: border-box;
  }

  .hero-image {
    position: absolute;
    top: 0;
    right: 0;
    width: 50%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
  }

  .hero-image::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 1;

    background:
      linear-gradient(
        to right,
        rgba(242, 247, 253, 1) 0%,
        rgba(242, 247, 253, 0.98) 8%,
        rgba(242, 247, 253, 0.90) 18%,
        rgba(242, 247, 253, 0.70) 30%,
        rgba(242, 247, 253, 0.40) 44%,
        rgba(242, 247, 253, 0.16) 58%,
        rgba(242, 247, 253, 0) 74%
      );
  }

  .hero-image::after {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 2;

    background:
      linear-gradient(
        to bottom,
        rgba(242, 247, 253, 0.08) 0%,
        rgba(242, 247, 253, 0) 45%,
        rgba(242, 247, 253, 0.12) 100%
      );
  }

  .hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .contenido {
    position: relative;
    z-index: 3;
    width: 100%;
    max-width: 820px;
    margin: 0 auto;
    transform: translateX(-14vw);
    text-align: center;
  }

  .subtitulo {
    margin: 0 0 1.2rem;
    text-transform: uppercase;
    letter-spacing: 0.3rem;
    color: var(--color-primary);
    font-size: 0.85rem;
    font-weight: 700;
  }

  .marca-hero {
    margin: 0;
    color: #1f2937;
    font-size: clamp(3rem, 7vw, 5rem);
    font-weight: 800;
    letter-spacing: -0.05em;
    line-height: 1;

    text-shadow:
      -1px -1px 1px rgba(255, 255, 255, 0.95),
      2px 2px 3px rgba(31, 41, 55, 0.16);
  }

  .marca-hero span {
    color: #2563eb;
  }

  .propuesta {
    max-width: 700px;
    margin: 2.5rem auto 0;
  }

  .frase-principal {
    margin: 0;
    color: var(--color-text);
    font-size: clamp(1.35rem, 3vw, 2rem);
    line-height: 1.35;
    letter-spacing: -0.02em;
  }

  .frase-secundaria {
    margin: 0.25rem 0 0;
    color: var(--color-text);
    font-size: clamp(1.35rem, 3vw, 2rem);
    font-weight: 700;
    line-height: 1.35;
    letter-spacing: -0.02em;
  }

  .descripcion {
    max-width: 650px;
    margin: 1.8rem auto 2.5rem;
    color: var(--color-text-light);
    font-size: clamp(1.05rem, 2vw, 1.2rem);
    line-height: 1.8;
  }

  .boton {
    display: inline-block;
    background: var(--color-primary);
    color: white;
    padding: 0.9rem 1.8rem;
    border-radius: var(--radius);
    text-decoration: none;
    font-weight: 700;
    transition:
      transform 0.2s ease,
      background 0.2s ease,
      box-shadow 0.2s ease;
  }

  .boton:hover {
    background: #1d4ed8;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(37, 99, 235, 0.2);
  }

  @media (max-width: 800px) {
    .hero {
      min-height: auto;
      padding: 5rem 1.5rem;
    }

    .hero-image {
      width: 100%;
      height: 42%;
      top: auto;
      bottom: 0;
      opacity: 0.24;
    }

    .hero-image::before {
      background:
        linear-gradient(
          to bottom,
          rgba(242, 247, 253, 1) 0%,
          rgba(242, 247, 253, 0.92) 15%,
          rgba(242, 247, 253, 0.65) 32%,
          rgba(242, 247, 253, 0.25) 55%,
          rgba(242, 247, 253, 0) 78%
        );
    }

    .hero-image::after {
      background: none;
    }

    .contenido {
      max-width: 650px;
      transform: none;
    }

    .subtitulo {
      letter-spacing: 0.2rem;
    }

    .marca-hero {
      font-size: clamp(2.8rem, 13vw, 4rem);
    }

    .propuesta {
      margin-top: 2rem;
    }

    .frase-principal,
    .frase-secundaria {
      font-size: 1.35rem;
    }

    .frase-secundaria {
      margin-top: 0.2rem;
    }

    .descripcion {
      margin-top: 1.7rem;
      line-height: 1.6;
    }
  }
</style>